# Core HTTP + AI providers
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.0

# Image processing (forensics LSB, bit-planes, steg)
Pillow>=10.0.0

# Crypto primitives (AES, RSA helpers etc.)
pycryptodome>=3.20.0

# Optional but recommended
# pwntools>=4.12.0        # binary / pwn challenges
# z3-solver>=4.12.0       # constraint solving (crypto/re)